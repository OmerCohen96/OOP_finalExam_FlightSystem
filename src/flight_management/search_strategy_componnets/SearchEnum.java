package flight_management.search_strategy_componnets;

/**
 Define the criteria / strategy for searching,
 use to define instances in the search factory.
 useful in the main search function .
 */
public enum SearchEnum {
    BY_DATE,
    BY_DESTINATION,
    BY_PRICE
}
